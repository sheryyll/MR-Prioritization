# Exact versions verified locally (CPU) - used to pin Kaggle/Colab notebook installs.
# Kaggle GPU notebooks already ship a CUDA-enabled torch; do NOT reinstall torch/torchvision
# there unless a version mismatch requires it. This file mainly pins the non-torch stack.

numpy==2.4.4
opencv-python==5.0.0.93
scikit-learn==1.9.0
scipy==1.18.0
matplotlib==3.11.1
seaborn==0.13.2
pandas==3.0.5
tqdm==4.70.0
pillow==12.2.0